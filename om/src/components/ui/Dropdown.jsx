import React from 'react';

const Dropdown = () => (
    <select className="h-10 px-3 border rounded-md text-sm">
        <option>Sort by</option>
        <option>Date</option>
        <option>Name</option>
    </select>
);

export default Dropdown;
